ports:
  - name: mbedtls
    tests: '{{ env.LONG_TEST | default(false) }}'
  - name: coremark_pro
    if: '{{ env.LONG_TEST | default(false) }}'
  - name: vsx_phoenix
    if: '{{ env.LONG_TEST | default(false) }}'
    tests: true

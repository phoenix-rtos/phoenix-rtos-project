# vim:sw=2:ts=2
name: ci

on:
  push:
    branches:
      - master
      - 'feature/*'
  pull_request:
    branches:
      - master
      - 'feature/*'
      - maska989/leon3_test

jobs:
  call-ci:
    uses: ./.github/workflows/ci-project.yml
    secrets: inherit

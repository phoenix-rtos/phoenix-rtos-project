tests: {{ bool(env.LONG_TEST) }}
ports:
  - name: azure_sdk
    {% if bool(env.LONG_TEST) %}
    use: [longtest]
    tests: true
    {% endif %}
  - name: busybox
    tests: true
  - name: curl
  - name: dropbear
  - name: fs_mark
  - name: lighttpd
  - name: lzo
  - name: micropython
    use: {{ ["longtest"] if bool(env.LONG_TEST) }}
    tests: true
  - name: openvpn
  - name: pcre
  - name: sscep
  - name: wpa_supplicant
  - name: heatshrink
